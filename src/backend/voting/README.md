HELLO THERE

ignore exampleInput.json

If you are working on generation layer. look at exampleBatchInput on the format that the code expects to take in.

to use, pass the array (as formated as shwon in exampleBatchInput), and pasa an array of the providers to use (the ai used to vote)
output will be in the format of exapleOutput
